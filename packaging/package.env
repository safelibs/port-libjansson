# Safelibs port metadata for libjansson.
#
# This port builds via safe/scripts/build-deb.sh. The DEB_* fields below
# are template-required scaffolding and are not consumed by the build.

SAFELIBS_LIBRARY="libjansson"

DEB_PACKAGE="safelibs-port-libjansson"
DEB_VERSION="0.0.0"
DEB_ARCHITECTURE="auto"
DEB_MAINTAINER="SafeLibs <ci@safelibs.org>"
DEB_SECTION="libs"
DEB_PRIORITY="optional"
DEB_DESCRIPTION="Safe libjansson port (built via safe/scripts/build-deb.sh)."
DEB_INSTALL_PREFIX="/usr/lib/safelibs/safelibs-port-libjansson"
DEB_DEPENDS=""
