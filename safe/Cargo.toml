[package]
name = "jansson-safe"
version = "2.14.0"
edition = "2021"
license = "MIT"
publish = false
build = "build.rs"

[lib]
name = "jansson"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
libc = "0.2"

[profile.release]
panic = "abort"
